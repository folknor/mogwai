# Fixture for scripts/probe_warmup_window.py.
#
# Mirrors the shape of the `ba forward` runs that hit the empty-warmup failure:
# an accelerated clock anchored at a fixed simulated epoch (2023-11-20T00:00:00Z)
# so the warmup window the bridge asks for lands on the same stretch of tape a
# QA run would see.
sim_epoch_ns = 1700438400000000000
speed = 60.0
gap_cap_ms = 1000
server_heartbeat_ms = 0
