# SPDX-FileCopyrightText: 2026 folknor
# SPDX-License-Identifier: AGPL-3.0-only

"""Offline analysis: corpus characterization and fingerprint synthesis.

A package only so the tests can import these modules by a stable path
(`python3 -m unittest discover -s analysis -t .` from the repo root). The
scripts here are still run directly and keep their flat sibling imports.
"""
