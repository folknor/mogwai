{
  "_standing": "FROZEN 2026-08-05 before any delivered byte of job GLBX-20260805-JUBCRPRLG8 was read. The machine-readable half of notes/pair-test-preregistration.md: the pair harness LOADS this file rather than mirroring it in code, so the prose and the executed rule cannot drift apart. Changing any value after the seal is opened is a recorded deviation.",
  "prereg_schema_version": 1,
  "job_id": "GLBX-20260805-JUBCRPRLG8",
  "session": {
    "expected_sessions": 10,
    "min_usable_sessions": 8,
    "first_trade_date": "2026-07-06",
    "last_trade_date": "2026-07-17",
    "timezone": "America/Chicago",
    "open_local": "17:00",
    "close_local": "16:00",
    "halt_local": ["15:15", "15:30"]
  },
  "inference": {
    "parent_rule": "contiguous rows sharing event timestamp and aggressor side, within one leg",
    "event_clock": "exchange event timestamp, never receive timestamp",
    "max_unsided_frac": 0.01,
    "closed_interval_gaps": "excluded from duration targets, counted"
  },
  "tolerances": {
    "ratio_tol_log": 0.22314355131420976,
    "ratio_tol_note": "log(1.25), 25 percent",
    "fraction_tol": 0.10,
    "correlation_tol": 0.10,
    "robust_mult": 1.5,
    "robust_min_frac": 0.70
  },
  "families": {
    "P1_cadence_level": {
      "mandatory": false,
      "targets": {"mean_event_duration_s": "ratio"}
    },
    "P2_duration_shape": {
      "mandatory": true,
      "targets": {
        "duration_dispersion_cv2": "ratio",
        "duration_acf_lag1": "correlation",
        "duration_acf_lag5": "correlation"
      }
    },
    "P3_sweep": {
      "mandatory": false,
      "targets": {
        "children_mean": "ratio",
        "children_single_frac": "fraction",
        "levels_mean": "ratio"
      }
    },
    "P4_return_shape": {
      "mandatory": true,
      "targets": {
        "return_acf_lag1": "correlation",
        "abs_acf_lag1": "correlation",
        "abs_acf_lag10": "correlation",
        "abs_acf_lag50": "correlation"
      }
    },
    "P5_zero_change": {
      "mandatory": true,
      "targets": {"zero_change_frac": "fraction"}
    },
    "P6_aggressor": {
      "mandatory": false,
      "probation": "no validated estimator lineage; counted in the majority, never mandatory",
      "targets": {
        "side_flip_prob": "fraction",
        "side_run_length_mean": "ratio"
      }
    }
  },
  "aggregate_rule": {
    "family_pass": "strict majority of the family's own targets",
    "verdict_pass": "all mandatory families pass AND strict majority of all evaluated families pass",
    "unavailable": "always fails the target, never a number",
    "insufficient_data": "fail",
    "partial_verdicts": "none"
  },
  "verdict_semantics": {
    "fail": "permanently skips wave 2A",
    "pass": "unlocks wave 2A without authorizing it",
    "wave_2b": "available under either verdict, separately gated",
    "excluded_evidence": "all quote and spread claims; all secondary measurements including size moments, per-second counts, notional"
  },
  "volume_vs_count_correction": {
    "part_of_verdict": false,
    "leg": "MNQ primary, NQ as cross-check",
    "output": "hour-of-day count and volume curves over open minutes, peak-to-trough ratios, ratio of ratios against the fitted 27.51"
  }
}
