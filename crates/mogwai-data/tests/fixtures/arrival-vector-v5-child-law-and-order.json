{
  "vector": "V5",
  "version": 2,
  "what_it_checks": "The SweepShape child law and the separate stochastic contracts' draw ordering.",
  "derivation": "For the geometric tail used by SweepShape with single probability s and continuation probability c, P(N=1)=s, P(N=2)=(1-s)c, P(N=3)=(1-s)c^2 and P(N=4)=(1-s)c^3. The values below use s=1/2 and c=1/2, giving 1/2, 1/4, 1/8 and 1/16. Section 7 places this draw after price/book work on contract A, and inside next_parent after the budget and the latent updates on contract B. Version 2 renamed contract A's two middle stages from price/book to latent_mid/side_book so they name the shipped stages the generator records, and both orders are now compared against production rather than against a literal in the test: contract A against the generator's own stage trace, contract B against a reference RNG consumed in the order stated below.",
  "params": { "single_fraction": 0.5, "continuation_probability": 0.5 },
  "inputs": [ { "n": [1, 2, 3, 4] } ],
  "expected": [ { "probability": ["0x3fe0000000000000", "0x3fd0000000000000", "0x3fc0000000000000", "0x3fb0000000000000"], "contract_a_order": ["gap", "flip", "latent_mid", "side_book", "child"], "contract_b_order": ["budget", "latent_steps", "child"] } ]
}
