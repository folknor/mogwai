# SPDX-FileCopyrightText: 2026 folknor
# SPDX-License-Identifier: AGPL-3.0-only

seed = 42
warmup_ns = 300000000000
speed = 0.0
# The PER-CONNECTION queue is what this fixture pins, so the process-wide
# permit is left at its default: a refusal here can only come from the socket's
# own bounded queue, not from the run-wide bound sitting in front of it.
pending_command_acts = 1
global_pending_command_acts = 4096
